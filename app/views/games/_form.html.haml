- semantic_form_for @game, :html => {:multipart => true} do |f|
  - f.inputs do
    = f.input :name
    = f.input :logo, :as => :file
    = f.input :description, :input_html => {:rows => 5}
  - f.buttons do
    = submit_button("#{image_tag("tick.png")} Submit")